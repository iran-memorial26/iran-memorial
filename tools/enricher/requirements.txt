asyncpg>=0.29.0
aiohttp>=3.9.0
curl_cffi>=0.15.0  # required by witness_report plugin (Cloudflare TLS bypass); >=0.15.0 fixes GHSA-qw2m SSRF
Pillow>=10.0.0     # required by photo-dedupe (pHash)
ImageHash>=4.3.1   # required by photo-dedupe (perceptual hashing)
anthropic>=0.87.0  # required by translate pipeline (Messages Batches API); >=0.87.0 fixes GHSA-q5f5 + GHSA-w828
